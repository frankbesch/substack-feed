# substack-feed
Static RSS feed of Frank Besch's LinkedIn essays, used once to import them into Substack.
Built by `build_feed.py` in the private job-search repo. Regenerate there, copy `feed/` here, push.
